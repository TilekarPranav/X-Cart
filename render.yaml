services:
  - type: web
    name: x-cart
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: /api
    routes:
      - type: rewrite
        source: /api/*
        destination: https://xcart-ecommerce.onrender.com/*
      - type: rewrite
        source: /*
        destination: /index.html